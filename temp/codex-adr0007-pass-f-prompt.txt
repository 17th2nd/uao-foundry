You are the independent, read-only ratifier for the UAO Foundry programme (Claude implements, Codex ratifies, git/runtime is authority). Pass F on branch feature/enrichment-operation: review `git diff HEAD~6 HEAD` (all six commits of ADR-0007) with emphasis on the sixth commit, which remediates your pass-E refusal (temp/codex-uaofoundry-adr0007-ratification-pass-e-001.md, findings F-E1..F-E5).

Ratify or refuse, citing file:line you have read yourself:
1. F-E1: scripts/enrich/bundle_lib.py SourcePool.install_origin is now two-phase (plan every id of an origin, then rewrite each reference once through the plan). Reproduce your base-first probe through BOTH callers (scripts/enrich/build_enrichment_bundle.py, scripts/exp002/reconcile_reuse.py) with the same record list for the whole origin. Any order or topology where a reference is moved twice, lands on other bytes, or where distinct bytes share an id?
2. F-E2/F-E3: bundle_lib.restate_identity now serves both builders — identity fields from the first registry candidate for the key, claims/evidence from ALL candidates, once per identity. Any dropped or duplicated registered assertion for a registry package with several candidates of one key, or a live package with several candidates of one key, or both at once?
3. F-E4: RegistryAwareCommandProvider carries constraints.enrichmentTargets (from OperatorConsole --enrich, sorted, absent when empty) and adapters/claude-code/claude_provider.py demands verbatim restatement plus new sourced claims for exactly those uids. Is the envelope byte-identical for non-enrichment manufactures? Is the adapter's rule consistent with ReuseAnalyzer's --enrich acceptance (strict superset, verbatim canonical-JSON equality) and with the bundle-builder fallback when the provider re-words? Any path where the instruction and the gate disagree?
4. F-E5: is `git diff --check HEAD~6 HEAD` clean, and are the counts in docs/ADR-0007-IDENTITY-ENRICHMENT.md accurate (`python3 -m unittest discover -s scripts/enrich/tests` expected 36; `python3 -m unittest adapters/claude-code/tests/test_claude_provider.py` expected 14; Java 182 per target/surefire-reports)?
5. Any defect the author missed across the whole ADR-0007 change set, including the six-pass ratification record's accuracy.
Output: verdict line (RATIFIED / RATIFIED WITH CONDITIONS / REFUSED), findings F-F1.. with severity and file:line, one-paragraph closure. Do not modify any file.
