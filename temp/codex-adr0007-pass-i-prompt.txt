You are the independent, read-only ratifier for the UAO Foundry programme (Claude implements, Codex ratifies, git/runtime is authority). Pass I on branch feature/enrichment-operation: review `git diff HEAD~9 HEAD` (all nine commits of ADR-0007) with emphasis on the ninth commit, which remediates your pass-H refusal (temp/codex-uaofoundry-adr0007-ratification-pass-h-001.md, findings F-H1, F-H2).

Known condition, stated up front: target/uao-foundry-0.1.0.jar is deliberately NOT rebuilt from this commit — a live manufacture batch (~150 seeds, the `ai` collection) holds the jar and the programme rule forbids rebuilding it while a manufacture is in flight. The jar is the eighth-commit build (702af40). Ratify the SOURCE and its tests; if everything else holds, state the jar rebuild as the single condition rather than refusing on it.

Ratify or refuse, citing file:line you have read yourself:
1. F-H1: FoundryRegistry.enforceWholePackageRule runs on every index build after applyEnrichment. Reproduce your register()-then-applyIdentityOperation(ENRICH) probe against current source (target/classes): is the journal entry refused and removed, the registry byte-identical, and is there any path — enrich(), register()+applyIdentityOperation(), a hand-written journal file read by index() — that yields a verified index with the subject SINGLE_VARIANT and another identity in the enriching package MULTIPLE_UNRECONCILED_VARIANTS? Is the "accepted lineage" definition (IdentityAggregate.inLineage: single state when never enriched, else the ENRICH chain's variants) sound, order-independent across operation ids, and does it wrongly refuse any legitimate history (e.g. an identity enriched later by a different package)?
2. F-H2: does schemas/identity-operation.schema.json now constrain ENRICH to exactly one subject and one target and state the other-identity rule, and does the Java constructor agree?
3. Are the ADR's nine-pass ratification record and counts accurate (`python3 -m unittest discover -s scripts/enrich/tests` expected 36; adapter 14; Java 186 per target/surefire-reports; `git diff --check HEAD~9 HEAD`)?
4. Any defect the author missed across the whole ADR-0007 change set.
Output: verdict line (RATIFIED / RATIFIED WITH CONDITIONS / REFUSED), findings F-I1.. with severity and file:line, one-paragraph closure. Do not modify any file.
