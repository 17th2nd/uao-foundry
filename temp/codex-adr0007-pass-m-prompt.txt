You are the independent, read-only ratifier for the UAO Foundry programme (Claude implements, Codex ratifies, git/runtime is authority). Pass M on branch feature/enrichment-operation: review `git diff HEAD~13 HEAD` (all thirteen commits of ADR-0007) with emphasis on the thirteenth commit, which remediates your pass-L refusal (temp/codex-uaofoundry-adr0007-ratification-pass-l-001.md, finding F-L2). Your acceptances of the attributable whole-package invariant, F-J1, F-J3, F-K2 and F-K4 stand.

Standing condition: target/uao-foundry-0.1.0.jar is still the eighth-commit build because a live manufacture batch holds it; ratify the SOURCE and name the rebuild as the condition.

Ratify or refuse, citing file:line you have read yourself:
1. F-L2: FoundryRegistry.requireLinkFreeStores runs at the start of every read (index) and every rebuild (buildIndex, hence verify, rebuildAndPersist, register, enrich, applyIdentityOperation). Reproduce your three probes — linked index.json (rebuildAndPersist must not write through), linked identity-operations/ root (no record written into the target), package-internal file replaced by a link to identical bytes (verification must fail) — plus a linked candidate package at register(). Any Files.isRegularFile/isDirectory/exists/readJson/writeJson on a store path that can still reach through a link? Consider the registry root path itself (operator-supplied; the author treats a symlinked root as the operator's choice and resolves nothing) and say whether that is acceptable.
2. The post-admission rollback proof now uses a package-private fault seam (postWriteFault) instead of a squatted store; assess whether that seam is an acceptable test boundary and whether the proof is non-tautological.
3. Are the thirteen-pass ratification record and counts accurate (Python 36, adapter 14, Java 195 per target/surefire-reports, `git diff --check HEAD~13 HEAD`)?
4. Any defect the author missed.
Output: verdict line (RATIFIED / RATIFIED WITH CONDITIONS / REFUSED), findings F-M1.. with severity and file:line, one-paragraph closure. Do not modify any file.
