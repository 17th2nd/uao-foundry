You are the independent, read-only ratifier for the UAO Foundry programme (Claude implements, Codex ratifies, git/runtime is authority). Pass H on branch feature/enrichment-operation: review `git diff HEAD~8 HEAD` (all eight commits of ADR-0007) with emphasis on the eighth commit, which remediates your pass-G refusal (temp/codex-uaofoundry-adr0007-ratification-pass-g-001.md, findings F-G1, F-G2, F-G6).

Ratify or refuse, citing file:line you have read yourself:
1. F-G1: is the one-identity-per-package invariant now held at every boundary — Options (any repeated --enrich), ReuseAnalyzer.analyze (ENRICHMENT_ONE_TARGET), and FoundryRegistry.enrich's whole-package preflight (every other registered identity the package carries must be a verbatim re-observation of its current state, checked before anything is written)? Reproduce your A-enrichment-plus-divergent-B probe against current source and the rebuilt jar. Any remaining path to a half-applicable admission or to a second identity becoming MULTIPLE_UNRECONCILED_VARIANTS through ENRICH?
2. F-G2: target/uao-foundry-0.1.0.jar was rebuilt from HEAD (08:46 local, 184/184 tests). Confirm the jar's FoundryRegistry carries identityContinuityDefect and the whole-package preflight, and that scripts/enrich/build_enrichment_bundle.py's default classpath therefore executes the current law.
3. F-G6: do the Decision in docs/ADR-0007-IDENTITY-ENRICHMENT.md, docs/REGISTRY.md and schemas/identity-operation.schema.json now state the two-half law (identity continuity + strict assertion superset) and the one-identity rule, consistently with the code?
4. Are the ADR's eight-pass ratification record and counts accurate (`python3 -m unittest discover -s scripts/enrich/tests` expected 36; adapter tests expected 14; Java 184 per target/surefire-reports; `git diff --check HEAD~8 HEAD`)?
5. Any defect the author missed across the whole ADR-0007 change set.
Output: verdict line (RATIFIED / RATIFIED WITH CONDITIONS / REFUSED), findings F-H1.. with severity and file:line, one-paragraph closure. Do not modify any file.
