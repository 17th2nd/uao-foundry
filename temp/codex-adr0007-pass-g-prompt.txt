You are the independent, read-only ratifier for the UAO Foundry programme (Claude implements, Codex ratifies, git/runtime is authority). Pass G on branch feature/enrichment-operation: review `git diff HEAD~7 HEAD` (all seven commits of ADR-0007) with emphasis on the seventh commit, which remediates your pass-F refusal (temp/codex-uaofoundry-adr0007-ratification-pass-f-001.md, findings F-F1..F-F4).

Ratify or refuse, citing file:line you have read yourself:
1. F-F1: the enrichment law now has an identity-continuity half (FoundryRegistry.identityContinuityDefect: canonical label unchanged, aliases and external identifiers may only grow) applied at enrich() admission, in ReuseAnalyzer (ENRICHMENT_IDENTITY_REGRESSION) and on every index rebuild. Is it the same law in all three places, re-derived from package bytes? Can your female-bovine probe (label/alias regression through restate_identity) still be admitted anywhere? Does scripts/enrich/bundle_lib.py restate_identity now reproduce the canonical label, aliases and external identifiers exactly as canonicalisation (AcquisitionStages.identityResolution) would derive them from the registry package's candidates, for one candidate and for several?
2. F-F2: is evidence whose supportsCandidateRef names an identity candidate restated (retargeted to the live candidate) alongside claim evidence, through both builders?
3. F-F3: does Options refuse more than one --enrich, and is that consistent with RegistryApplication enrich's single subject? Any remaining path to a half-applicable enrichment?
4. F-F4: is the reused-identity rule (no new claim unless an enrichment target; names and identifiers kept) now emitted by adapters/claude-code/claude_provider.py with and without a relationship edition, and consistent with the reuse gate in both modes?
5. Are the ADR's seven-pass ratification record and counts accurate (`python3 -m unittest discover -s scripts/enrich/tests` expected 36; adapter tests expected 14; Java 183 per target/surefire-reports; `git diff --check HEAD~7 HEAD`)?
6. Any defect the author missed across the whole ADR-0007 change set.
Output: verdict line (RATIFIED / RATIFIED WITH CONDITIONS / REFUSED), findings F-G1.. with severity and file:line, one-paragraph closure. Do not modify any file.
