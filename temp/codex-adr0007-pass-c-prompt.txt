You are the independent, read-only ratifier for the UAO Foundry programme (Claude implements, Codex ratifies, git/runtime is authority). Pass C on branch feature/enrichment-operation: review `git diff HEAD~3 HEAD` (all three commits of ADR-0007) with emphasis on the third commit, which remediates your pass-B refusal (temp/codex-uaofoundry-adr0007-ratification-pass-b-001.md, findings F-B2, F-B3, F-B5, F-B6, F-B7).

Ratify or refuse, citing file:line you have read yourself:
1. F-B2: do scripts/exp002/reconcile_reuse.py and scripts/enrich/build_enrichment_bundle.py now select the currentVariant occurrence via scripts/enrich/bundle_lib.py current_occurrence, and does that helper fail closed (unreconciled variants, dangling pointer) rather than guess? Any remaining reader that takes occurrences[0]?
2. F-B3: does enrichLeavesTheRegistryByteIdenticalOnRefusalBeforeAndAfterAdmission now genuinely cross admission (register() succeeds, only the ENRICH record write fails), and are its postconditions non-tautological (package removed, index byte-identical, empty journal, the same enrichment succeeds afterwards)? Is the Assumptions guard sound, or could it silently skip on CI?
3. F-B5: does split_paraphrases catch a reworded prior assertion, and does the bundle builder refuse it unless the operator names it with --accept-paraphrase, recording that in authorityNotes? Is the added==0 guard now reached when nothing genuine remains? Assess the threshold/overlap design for false negatives you can construct.
4. F-B6: does SourcePool prevent live bytes from standing behind historical claims (registry sources first under their package, colliding live ids renamed with sourceRefs/sourceRef rewritten, registry-vs-registry shared only on equal sha256)? Any path where a reference is left pointing at the wrong source, or a renamed id collides again?
5. F-B7: does the console Report read enrichment back from the analyzer's enrichedUaos, and do non-enrichment reports stay byte-identical?
6. Any defect the author missed, including in the 13 Python unit tests (scripts/enrich/tests/test_bundle_lib.py: `python3 -m unittest discover -s scripts/enrich/tests`) and the Java suite (181/181 reported).
Output: verdict line (RATIFIED / RATIFIED WITH CONDITIONS / REFUSED), findings F-C1.. with severity and file:line, one-paragraph closure. Do not modify any file.
